import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tarea-3-Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
